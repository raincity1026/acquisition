# 服务器侧 nginx：只做 api.stocks.marsian.cn 的 API 网关
# （前端在 EdgeOne，不在这里托管静态文件）。
# CORS 由后端 FastAPI 处理，这里只反代 + 终结 HTTPS。

server {
    listen 80;
    server_name api.stocks.marsian.cn;

    # Let's Encrypt 验证
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        proxy_pass http://backend:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 120s;
    }
}

# === 签好证书后启用 HTTPS（取消注释）===
# server {
#     listen 443 ssl;
#     http2 on;
#     server_name api.stocks.marsian.cn;
#     ssl_certificate     /etc/letsencrypt/live/api.stocks.marsian.cn/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/api.stocks.marsian.cn/privkey.pem;
#
#     location / {
#         proxy_pass http://backend:8000;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_read_timeout 120s;
#     }
# }
